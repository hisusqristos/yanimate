describe("tool to animate. we might have frames, animation duration and frame frequency",()=>{
    //Frames as an array,
    // duration as a number (10 for 10 seconds)
    // frame frequency as a number (12 for 12 frames per second)
    describe("Animate(ArrarOfFrames, 10, 24)") // for example
})